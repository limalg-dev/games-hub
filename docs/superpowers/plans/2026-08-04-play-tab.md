# Play Tab Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Abrir cada jogo numa aba própria com URL compartilhável, onde a partida só começa ao clicar Play.

**Architecture:** `GET /play/{game}` devolve a mesma `static/index.html` de `/`. No boot, `app.js` lê o caminho, pula a landing e mostra a `#game-view` com um overlay de Play; o clique chama as funções de início que já existem. A construção e a leitura da URL saem para um módulo novo sem DOM, para poderem ser testadas em node.

**Tech Stack:** FastAPI, SQLModel, JavaScript ES modules sem bundler, pytest, node (para exercitar os módulos do navegador).

## Global Constraints

- Os valores de configuração na URL usam os nomes que o código já consome: `difficulty` (`easy` | `medium` | `hard`) e `category` (`random` | `animals` | `countries` | `tech` | `food` | `sports`).
- Padrões quando o parâmetro falta ou é inválido: `difficulty=easy`, `category=random`.
- Jogos válidos: `checkers`, `wordsearch`, `crossword`.
- O portão de Play vale igual para os três jogos.
- Nenhuma partida é criada no servidor antes do clique em Play.
- Testes em node fazem `skip` quando o node não está instalado, seguindo `tests/test_wordsearch_grid.py`.
- Rodar a suíte com `.venv/bin/python -m pytest -q`.

---

### Task 1: Rota `/play/{game}`

**Files:**
- Modify: `app/main.py:53-55`
- Test: `tests/test_play_route.py`

**Interfaces:**
- Consumes: nada
- Produces: `GET /play/{game}` devolvendo o corpo de `static/index.html` com status 200, ou 404 para jogo desconhecido.

- [ ] **Step 1: Write the failing test**

Create `tests/test_play_route.py`:

```python
import pytest
from fastapi.testclient import TestClient
from app.main import app, seed_words
from app.database import engine, init_db
from sqlmodel import SQLModel


@pytest.fixture(autouse=True)
def _setup_db():
    init_db()
    seed_words()
    yield
    with engine.connect() as conn:
        trans = conn.begin()
        for table in reversed(SQLModel.metadata.sorted_tables):
            conn.execute(table.delete())
        trans.commit()


client = TestClient(app)


@pytest.mark.parametrize("game", ["checkers", "wordsearch", "crossword"])
def test_play_serves_the_same_document_as_root(game):
    resp = client.get(f"/play/{game}")
    assert resp.status_code == 200
    assert resp.text == client.get("/").text


def test_play_rejects_an_unknown_game():
    assert client.get("/play/xadrez").status_code == 404


def test_play_ignores_query_string():
    resp = client.get("/play/wordsearch?difficulty=hard&category=animals")
    assert resp.status_code == 200
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/test_play_route.py -q`
Expected: FAIL — as três rotas devolvem 404, porque `/play/{game}` não existe.

- [ ] **Step 3: Write minimal implementation**

In `app/main.py`, right after the existing `root()` handler (line 53-55), add:

```python
PLAYABLE_GAMES = ("checkers", "wordsearch", "crossword")


@app.get("/play/{game}")
async def play(game: str):
    if game not in PLAYABLE_GAMES:
        raise HTTPException(status_code=404, detail="Unknown game")
    return FileResponse("static/index.html")
```

`HTTPException` and `FileResponse` are already imported at the top of the file.

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/test_play_route.py -q`
Expected: PASS (5 tests)

- [ ] **Step 5: Commit**

```bash
git add app/main.py tests/test_play_route.py
git commit -m "feat: serve the landing document at /play/{game}"
```

---

### Task 2: Módulo de URL

Constrói e lê as URLs de `/play`. Fica num arquivo próprio, sem tocar no DOM, para ser exercitável em node — `static/app.js` acessa o DOM no topo e não pode ser importado fora do navegador.

**Files:**
- Create: `static/play-url.js`
- Test: `tests/test_play_url.py`

**Interfaces:**
- Consumes: nada
- Produces:
  - `buildPlayUrl(game, options = {})` → string. `options` aceita `difficulty` e `category`. Só inclui na query os valores que diferem do padrão.
  - `parsePlayUrl(pathname, search = '')` → `{ game, difficulty, category }` ou `null` quando o caminho não é `/play/<jogo válido>`.
  - `PLAYABLE_GAMES` → array de strings.

- [ ] **Step 1: Write the failing test**

Create `tests/test_play_url.py`:

```python
"""Exercise the /play URL helpers the way the browser will."""
from __future__ import annotations

import json
import shutil
import subprocess
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parent.parent
STATIC = ROOT / "static"

HARNESS = """
const { buildPlayUrl, parsePlayUrl, PLAYABLE_GAMES } = await import('./play-url.js');

const results = {
  games: PLAYABLE_GAMES,
  plain: buildPlayUrl('checkers'),
  defaults: buildPlayUrl('wordsearch', { difficulty: 'easy', category: 'random' }),
  configured: buildPlayUrl('wordsearch', { difficulty: 'hard', category: 'animals' }),
  crossword: buildPlayUrl('crossword', { difficulty: 'medium' }),
  parsedPlain: parsePlayUrl('/play/checkers'),
  parsedConfigured: parsePlayUrl('/play/wordsearch', '?difficulty=hard&category=animals'),
  parsedBogusValues: parsePlayUrl('/play/wordsearch', '?difficulty=impossivel&category=nada'),
  parsedLanding: parsePlayUrl('/'),
  parsedUnknown: parsePlayUrl('/play/xadrez'),
};
console.log(JSON.stringify(results));
"""


@pytest.fixture(scope="module")
def result(tmp_path_factory):
    node = shutil.which("node")
    if node is None:
        pytest.skip("node is not installed")

    work = tmp_path_factory.mktemp("play_url")
    shutil.copy(STATIC / "play-url.js", work / "play-url.js")
    (work / "package.json").write_text('{"type":"module"}', encoding="utf-8")
    (work / "harness.mjs").write_text(HARNESS, encoding="utf-8")

    proc = subprocess.run(
        [node, "harness.mjs"], cwd=work, capture_output=True, text=True, timeout=120
    )
    assert proc.returncode == 0, f"harness failed:\\n{proc.stderr}"
    return json.loads(proc.stdout.strip().splitlines()[-1])


def test_lists_the_playable_games(result):
    assert result["games"] == ["checkers", "wordsearch", "crossword"]


def test_builds_a_bare_url_when_there_is_nothing_to_configure(result):
    assert result["plain"] == "/play/checkers"


def test_omits_values_that_match_the_defaults(result):
    assert result["defaults"] == "/play/wordsearch"


def test_includes_values_that_differ_from_the_defaults(result):
    assert result["configured"] == "/play/wordsearch?difficulty=hard&category=animals"
    assert result["crossword"] == "/play/crossword?difficulty=medium"


def test_parses_a_bare_url_into_defaults(result):
    assert result["parsedPlain"] == {
        "game": "checkers",
        "difficulty": "easy",
        "category": "random",
    }


def test_parses_a_configured_url(result):
    assert result["parsedConfigured"] == {
        "game": "wordsearch",
        "difficulty": "hard",
        "category": "animals",
    }


def test_falls_back_to_defaults_for_values_outside_the_allowed_set(result):
    assert result["parsedBogusValues"] == {
        "game": "wordsearch",
        "difficulty": "easy",
        "category": "random",
    }


def test_returns_null_outside_the_play_route(result):
    assert result["parsedLanding"] is None
    assert result["parsedUnknown"] is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/test_play_url.py -q`
Expected: FAIL no fixture — `harness failed` com `Cannot find module ... play-url.js`.

- [ ] **Step 3: Write minimal implementation**

Create `static/play-url.js`:

```js
export const PLAYABLE_GAMES = ['checkers', 'wordsearch', 'crossword'];

const DIFFICULTIES = ['easy', 'medium', 'hard'];
const CATEGORIES = ['random', 'animals', 'countries', 'tech', 'food', 'sports'];

const DEFAULTS = { difficulty: 'easy', category: 'random' };

function pick(value, allowed, fallback) {
  return allowed.includes(value) ? value : fallback;
}

export function buildPlayUrl(game, options = {}) {
  const params = new URLSearchParams();
  const difficulty = pick(options.difficulty, DIFFICULTIES, DEFAULTS.difficulty);
  const category = pick(options.category, CATEGORIES, DEFAULTS.category);
  // Only values that differ from the default earn a place in the URL, so a
  // shared link stays readable.
  if (difficulty !== DEFAULTS.difficulty) params.set('difficulty', difficulty);
  if (category !== DEFAULTS.category) params.set('category', category);
  const query = params.toString();
  return query ? `/play/${game}?${query}` : `/play/${game}`;
}

export function parsePlayUrl(pathname, search = '') {
  const match = /^\/play\/([^/?#]+)\/?$/.exec(pathname || '');
  if (!match) return null;
  const game = decodeURIComponent(match[1]);
  if (!PLAYABLE_GAMES.includes(game)) return null;
  const params = new URLSearchParams(search);
  return {
    game,
    difficulty: pick(params.get('difficulty'), DIFFICULTIES, DEFAULTS.difficulty),
    category: pick(params.get('category'), CATEGORIES, DEFAULTS.category),
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/test_play_url.py -q`
Expected: PASS (8 tests)

- [ ] **Step 5: Commit**

```bash
git add static/play-url.js tests/test_play_url.py
git commit -m "feat: add helpers to build and parse /play URLs"
```

---

### Task 3: Separar montagem de início no wordsearch

Hoje `WordSearchGame.init()` termina chamando `this.start()`, então o cronômetro dispara junto com o grid. O portão de Play só significa algo se os dois passos forem separados.

**Files:**
- Modify: `games/wordsearch/static/board.js:21-36`
- Test: `tests/test_wordsearch_lifecycle.py`

**Interfaces:**
- Consumes: nada
- Produces: `init()` monta o grid e deixa `isPlaying === false` e `timerInterval === null`; `start()` passa `isPlaying` para `true` e cria o intervalo.

- [ ] **Step 1: Write the failing test**

Create `tests/test_wordsearch_lifecycle.py`:

```python
"""init() must lay out the puzzle without starting the clock."""
from __future__ import annotations

import json
import shutil
import subprocess
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parent.parent
STATIC = ROOT / "games" / "wordsearch" / "static"

HARNESS = """
globalThis.document = { getElementById: () => ({}) };
globalThis.localStorage = {
  getItem: () => null,
  setItem: () => {},
  removeItem: () => {},
};

const { WordSearchGame } = await import('./board.js');

const game = new WordSearchGame({
  containerId: 'board-wrapper',
  gridSize: 10,
  wordCount: 6,
  directions: 4,
  category: 'animals',
});
// The DOM painting is not what this test is about.
game.render = () => {};

game.init();
const afterInit = { isPlaying: game.isPlaying, hasTimer: game.timerInterval !== null,
                    hasGrid: Array.isArray(game.grid) && game.grid.length === 10 };

game.start();
const afterStart = { isPlaying: game.isPlaying, hasTimer: game.timerInterval !== null };
clearInterval(game.timerInterval);

console.log(JSON.stringify({ afterInit, afterStart }));
"""


@pytest.fixture(scope="module")
def lifecycle(tmp_path_factory):
    node = shutil.which("node")
    if node is None:
        pytest.skip("node is not installed")

    work = tmp_path_factory.mktemp("wordsearch_lifecycle")
    for src in STATIC.glob("*.js"):
        shutil.copy(src, work / src.name)
    (work / "package.json").write_text('{"type":"module"}', encoding="utf-8")
    (work / "harness.mjs").write_text(HARNESS, encoding="utf-8")

    proc = subprocess.run(
        [node, "harness.mjs"], cwd=work, capture_output=True, text=True, timeout=120
    )
    assert proc.returncode == 0, f"harness failed:\\n{proc.stderr}"
    return json.loads(proc.stdout.strip().splitlines()[-1])


def test_init_lays_out_the_grid(lifecycle):
    assert lifecycle["afterInit"]["hasGrid"] is True


def test_init_does_not_start_the_clock(lifecycle):
    assert lifecycle["afterInit"]["isPlaying"] is False
    assert lifecycle["afterInit"]["hasTimer"] is False


def test_start_begins_play(lifecycle):
    assert lifecycle["afterStart"]["isPlaying"] is True
    assert lifecycle["afterStart"]["hasTimer"] is True
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/python -m pytest tests/test_wordsearch_lifecycle.py -q`
Expected: FAIL em `test_init_does_not_start_the_clock` — `isPlaying` é `True` e o intervalo já existe.

- [ ] **Step 3: Write minimal implementation**

In `games/wordsearch/static/board.js`, replace the last two lines of `init()`:

```js
    this.isPlaying = true; // Allow selection immediately after init
    this.render();
    this.start(); // Start timer automatically
  }
```

with:

```js
    // Laid out but not live: start() opens play and the clock, so a Play gate
    // can sit in between.
    this.isPlaying = false;
    this.render();
  }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `.venv/bin/python -m pytest tests/test_wordsearch_lifecycle.py -q`
Expected: PASS (3 tests)

- [ ] **Step 5: Run the whole suite**

Run: `.venv/bin/python -m pytest -q`
Expected: PASS. Nada mais chama `init()` esperando que o jogo comece — `startWordSearch` em `static/app.js` ganha o `start()` explícito na Task 5.

- [ ] **Step 6: Commit**

```bash
git add games/wordsearch/static/board.js tests/test_wordsearch_lifecycle.py
git commit -m "feat: separate word search layout from starting the clock"
```

---

### Task 4: Botão do modal aponta para a aba

**Files:**
- Modify: `static/index.html:50`
- Modify: `static/app.js` — o import no topo, o corpo de `openModal`, e o listener de `#modal-play-btn` (hoje em `modalPlayBtn?.addEventListener('click', () => startGame(STATE.selectedGame));`)

**Interfaces:**
- Consumes: `buildPlayUrl` da Task 2
- Produces: `#modal-play-btn` é uma âncora cujo `href` reflete a configuração escolhida no modal.

- [ ] **Step 1: Trocar o botão por uma âncora**

In `static/index.html`, replace line 50:

```html
        <button class="btn-primary btn-play-large" id="modal-play-btn">Jogar Agora</button>
```

with:

```html
        <a class="btn-primary btn-play-large" id="modal-play-btn" target="_blank" rel="noopener" href="/">Jogar Agora</a>
```

- [ ] **Step 2: Importar o construtor de URL**

In `static/app.js`, add to the imports at the top of the file:

```js
import { buildPlayUrl, parsePlayUrl } from '/static/play-url.js';
```

`parsePlayUrl` fica sem uso até a Task 5; deixá-lo aqui evita mexer no bloco de imports duas vezes.

- [ ] **Step 3: Atualizar o href sempre que a configuração mudar**

In `static/app.js`, add this helper right above `function openModal(gameId) {`:

```js
function refreshPlayLink() {
  if (!modalPlayBtn || !STATE.selectedGame) return;
  const difficulty =
    document.querySelector('input[name="ws-difficulty"]:checked')?.value ||
    document.querySelector('input[name="cw-difficulty"]:checked')?.value;
  const category = document.getElementById('ws-category')?.value;
  modalPlayBtn.href = buildPlayUrl(STATE.selectedGame, { difficulty, category });
}
```

At the end of `openModal(gameId)`, after the per-game configuration markup is injected, add:

```js
  refreshPlayLink();
  modalRules.addEventListener('change', refreshPlayLink);
```

`modalRules.innerHTML` é reescrito a cada `openModal`, o que descarta os nós antigos mas não o listener do próprio `modalRules`. Registrar de novo empilharia handlers; troque a linha acima por um registro único, feito uma vez no boot — mova `modalRules.addEventListener('change', refreshPlayLink);` para dentro de `function init()` e deixe apenas `refreshPlayLink();` no fim de `openModal`.

- [ ] **Step 4: Remover o início direto pela landing**

In `static/app.js`, delete this listener:

```js
modalPlayBtn?.addEventListener('click', () => startGame(STATE.selectedGame));
```

A âncora agora navega sozinha.

- [ ] **Step 5: Conferir no navegador**

```bash
.venv/bin/python -m uvicorn app.main:app --port 8910 &
```

Abra `http://127.0.0.1:8910/`, clique no card do wordsearch, escolha Difícil e Animais, e confirme que o botão aponta para `/play/wordsearch?difficulty=hard&category=animals`. Feche o servidor ao terminar.

- [ ] **Step 6: Commit**

```bash
git add static/index.html static/app.js
git commit -m "feat: point the modal Play button at the game tab"
```

---

### Task 5: Boot na aba de jogo

**Files:**
- Modify: `static/index.html` — overlay dentro de `#game-view`
- Modify: `static/app.js` — `function init()`, `startWordSearch`, e o listener de `btnBack`

**Interfaces:**
- Consumes: `parsePlayUrl` da Task 2, `start()` da Task 3
- Produces: `/play/{game}` abre direto na `#game-view` com o overlay; o Play inicia a partida.

- [ ] **Step 1: Adicionar o overlay**

In `static/index.html`, immediately after the opening tag of `<section id="game-view" ...>`, add:

```html
    <div id="play-gate" class="play-gate hidden">
      <div class="play-gate-inner">
        <h2 id="play-gate-title"></h2>
        <button class="btn-primary btn-play-large" id="play-gate-btn">Play</button>
      </div>
    </div>
```

- [ ] **Step 2: Estilizar o overlay**

Append to `static/styles.css`:

```css
.play-gate {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
  background: rgba(10, 12, 24, 0.82);
  z-index: 20;
}

.play-gate-inner {
  display: grid;
  gap: 1rem;
  justify-items: center;
  text-align: center;
}
```

`#game-view` precisa ser o bloco de referência do `inset`. Confirme que a regra de `#game-view` em `static/styles.css` tem `position: relative;` e acrescente se faltar.

- [ ] **Step 3: Iniciar o jogo pelo overlay**

In `static/app.js`, replace `function init()` with:

```js
function init() {
  renderGameGrid();
  renderFeaturedSpotlight();
  renderFeaturedSecondary();
  renderCollections();
  modalRules.addEventListener('change', refreshPlayLink);

  const play = parsePlayUrl(location.pathname, location.search);
  if (play) openPlayGate(play);
}

async function openPlayGate(play) {
  const gate = document.getElementById('play-gate');
  const title = document.getElementById('play-gate-title');
  const button = document.getElementById('play-gate-btn');
  STATE.selectedGame = play.game;
  STATE.currentGameType = play.game;
  STATE.playConfig = play;
  showView('game');
  title.textContent = GAMES[play.game].title;
  gate.classList.remove('hidden');

  // Só o wordsearch tem o que mostrar antes do Play: o grid montado aqui é o
  // mesmo que segue em jogo depois.
  if (play.game === 'wordsearch') {
    await prepareWordSearch(getWordSearchConfig());
  }

  button.addEventListener('click', () => {
    gate.classList.add('hidden');
    if (play.game === 'wordsearch') {
      beginWordSearch();
    } else if (play.game === 'crossword') {
      startCrossword(play.difficulty);
    } else {
      startGame(play.game);
    }
  }, { once: true });
}
```

- [ ] **Step 3b: Separar a montagem do wordsearch do seu início**

In `static/app.js`, replace `async function startWordSearch(config)` with the pair below, keeping `startWordSearch` as the combined path that `startGame` (and therefore New Game) still uses:

```js
let wordSearchTimer = null;

async function prepareWordSearch(config) {
  closeModal();
  showView('game');
  STATE.currentGameType = 'wordsearch';
  STATE.game.id = 'wordsearch-' + Date.now();

  wordSearchTimer = await import('/games/wordsearch/static/timer.js');
  const { WordSearchGame } = await import('/games/wordsearch/static/board.js');
  wordSearchGame = new WordSearchGame({ containerId: 'board-wrapper', ...config });
  wordSearchGame.onGameComplete = (time) => {
    wordSearchTimer.stopTimer();
    wordSearchTimer.saveScore(config, time * 1000);
    alert(`Parabéns! Você completou em ${formatTime(time * 1000)}`);
  };
  wordSearchGame.init();
  updateGameViewForWordSearch();
}

function beginWordSearch() {
  wordSearchGame.start();
  wordSearchTimer.startTimer((seconds) => {
    const el = document.getElementById('timer');
    if (el) el.textContent = formatTime(seconds * 1000);
  });
}

async function startWordSearch(config) {
  await prepareWordSearch(config);
  beginWordSearch();
}
```

- [ ] **Step 3c: A configuração da aba vence a do modal**

`getWordSearchConfig()` lê os controles do modal, que não existem na aba de jogo — sem isso o New Game numa aba `/play/wordsearch?difficulty=hard` reiniciaria no Fácil. In `static/app.js`, replace the body of `getWordSearchConfig` with:

```js
function getWordSearchConfig() {
  const play = STATE.playConfig;
  const difficulty = play?.difficulty
    || document.querySelector('input[name="ws-difficulty"]:checked')?.value
    || 'easy';
  const category = play?.category
    || document.getElementById('ws-category')?.value
    || 'random';
  const diff = DIFFICULTIES[difficulty];
  return { ...diff, difficulty, category };
}
```

- [ ] **Step 4: Dar ao crossword a dificuldade da URL**

In `static/app.js`, change the first line of `startCrossword` from

```js
async function startCrossword() {
  const difficulty = document.querySelector('input[name="cw-difficulty"]:checked')?.value || 'easy';
```

to

```js
async function startCrossword(difficultyFromUrl) {
  const difficulty = difficultyFromUrl
    || document.querySelector('input[name="cw-difficulty"]:checked')?.value
    || 'easy';
```

- [ ] **Step 5: Conferir que o cronômetro só parte no Play**

Nada a escrever aqui: o Step 3b já colocou `wordSearchGame.start()` dentro de
`beginWordSearch()`, que só roda no clique. Releia as duas funções e confirme
que `prepareWordSearch` não chama `start()` nem `startTimer` — é essa separação
que a Task 3 tornou possível.

- [ ] **Step 6: Voltar sai da aba de jogo**

In `static/app.js`, replace the back listener

```js
btnBack?.addEventListener('click', backToLanding);
```

with

```js
btnBack?.addEventListener('click', () => {
  // Uma aba aberta em /play não tem landing atrás dela para revelar.
  if (parsePlayUrl(location.pathname, location.search)) {
    location.href = '/';
    return;
  }
  backToLanding();
});
```

- [ ] **Step 7: Rodar a suíte**

Run: `.venv/bin/python -m pytest -q`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add static/index.html static/styles.css static/app.js
git commit -m "feat: open /play tabs behind a Play gate"
```

---

### Task 6: Verificação ponta a ponta

**Files:**
- Create: `.context/verify-play-tab.mjs` (arquivo de trabalho, não versionado — `.context/` é gitignored)

**Interfaces:**
- Consumes: tudo acima
- Produces: evidência de que a aba abre em espera e que nada começa antes do Play

- [ ] **Step 1: Escrever o roteiro de verificação**

Create `.context/verify-play-tab.mjs`:

```js
import { chromium } from 'playwright';

const base = process.argv[2];
const b = await chromium.launch({ channel: 'chrome' });

for (const [url, label] of [
  ['/play/checkers', 'checkers'],
  ['/play/wordsearch?difficulty=hard&category=animals', 'wordsearch'],
  ['/play/crossword?difficulty=medium', 'crossword'],
]) {
  const p = await b.newPage();
  const errs = [];
  let sockets = 0;
  p.on('pageerror', e => errs.push(e.message));
  p.on('websocket', () => sockets++);
  await p.goto(base + url, { waitUntil: 'networkidle' });

  const gateBefore = await p.locator('#play-gate').isVisible();
  const socketsBefore = sockets;
  await p.locator('#play-gate-btn').click();
  await p.waitForTimeout(2500);

  console.log(
    `${label.padEnd(11)} portao antes=${gateBefore} depois=${await p.locator('#play-gate').isVisible()} ` +
    `sockets antes=${socketsBefore} depois=${sockets} erros=${errs.length ? errs.join('|') : 'nenhum'}`
  );
  await p.close();
}
await b.close();
```

- [ ] **Step 2: Rodar**

```bash
.venv/bin/python -m uvicorn app.main:app --port 8911 &
sleep 4
node .context/verify-play-tab.mjs http://127.0.0.1:8911
```

Expected, para os três jogos: `portao antes=true depois=false`, `erros=nenhum`. Para checkers e crossword, `sockets antes=0` e `depois=1`. Para wordsearch, `sockets` fica em 0 nos dois — ele não usa WebSocket.

- [ ] **Step 3: Conferir o cronômetro do wordsearch**

Com o servidor ainda de pé, abra `http://127.0.0.1:8911/play/wordsearch?difficulty=hard&category=animals`, confirme que o `#timer` fica em `00:00` enquanto o overlay estiver visível, clique Play e confirme que passa a correr e que o grid é 15×15. Encerre o servidor.

- [ ] **Step 4: Commit final**

```bash
git add -A
git commit -m "chore: verify the play tab flow end to end"
```

Se nada tiver ficado por versionar, pule o commit.
